[tox]
envlist = py39, py310, py311, py312, py313
skip_missing_interpreters = true

[testenv]
deps =
    pytest>=8
    pytest-cov>=5
    requests-mock>=1.12
    hypothesis>=6
    .[db,xml]
commands =
    pytest --cov --cov-report=term-missing --cov-fail-under=85 {posargs}
